# Отзыв по проекту «project1» — бот с расписанием Лицея

## Зарина

**Что сделала:** интерфейс бота — `bot.py` и `keyboard.py` (клавиатуры, навигация, выбор класса → день недели, вывод расписания через `aiogram`).

**Сильные стороны:**
- Правильная асинхронность aiogram.
- Удобная двухуровневая навигация (класс → день).
- Логирование действий пользователей.

**Что можно улучшить:**
- Глобальный словарь `current_class` лучше заменить на `FSMContext` aiogram — это надёжнее.
- Токен стоит вынести в `.env`.
- Добавь обработку случая, когда для выбранного класса нет расписания.
- Аннотации типов (type hints) улучшат читаемость.

**Оценка: 4** — функционал работает, осталось немного навести порядок.
